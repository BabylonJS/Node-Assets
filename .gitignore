node_modules/

# pnpm is the package manager for this repository (see `packageManager` in package.json).
# npm and Yarn lockfiles diverge from pnpm-lock.yaml and are not committed.
package-lock.json
yarn.lock

dist/
docs/
test-results/
*.tsbuildinfo
.vite/

.env.local
.DS_Store

# Scratch space; see AGENTS.md.
_/
